name: Cron Pulse Test

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - name: Timestamp
        run: |
          echo "pulse $(TZ=Asia/Shanghai date '+%H:%M:%S')"
